void printInteger(int number) {
  // 변수의 사용을 위해 $variableName 을 사용
  print('The number is $number.');
}

void main() {
  var number = 1024; // Declare and Initailize a Variable
  printInteger(number);
}
